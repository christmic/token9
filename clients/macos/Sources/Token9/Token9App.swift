import SwiftUI

@main
struct Token9App: App {
    var body: some Scene {
        WindowGroup("token9") {
            StatsView()
                .frame(minWidth: 760, minHeight: 520)
        }
    }
}
